# 快速开始指南

## 第一次使用？按这个步骤来

### 步骤 1️⃣：编辑技能

1. **打开桌面工具** → 看到顶部有 3 个标签页
2. **点击 "技能编辑"** 标签（左数第二个）
3. **等待加载** → 系统自动加载 30+ 个技能的默认模版
4. **查看技能列表** → 每个技能显示为一张卡片：
   - 蓝色标签 = 技能所属阶段（白十字、两层、最后层定向、最后层排列、进阶）
   - 粗体标题 = 技能中文名称
   - 下面的细节 = 英文名、学习目标、前置技能要求、掌握标准

### 编辑/新建技能

**编辑现有技能**：
- 点击卡片上的 **`编辑`** 按钮
- 在弹出的表单中修改内容（中文名、英文名、学习目标、掌握标准）
- 点击 **`保存`** 确认

**新建技能**：
- 在工具栏"新建技能"区找到 **`+ 创建`** 按钮
- 选择技能所属的阶段
- 点击 **`+ 创建`**
- 会自动添加一个新技能，点击 **`编辑`** 完成信息填写

**删除技能**（谨慎操作）：
- 点击卡片上的 **`删除`** 按钮
- ⚠️ 如果其他技能依赖它，删除会失败

### 导出技能文件

1. **检查是否有未保存的改动** → 看右上角是否有 **`保存`** 按钮
2. 如果有，点击 **`保存`**
3. 点击右上角 **`导出 JSON`** 按钮
4. 在文件保存对话框中保存为 `skill_graph_cfop.json`
5. 把这个文件**覆盖**到 App 项目的 `data/skills/` 目录

---

### 步骤 2️⃣：分配关卡技能

1. **点击 "关卡映射"** 标签（左数第三个）
2. 看到所有 51 个关卡的卡片：
   - **灰色卡片** = 还没有分配技能
   - **白色卡片** = 已经分配了技能

### 分配技能给关卡

**方式 A：逐个分配（精确）**
- 找到未分配的卡片（灰色）
- 点击下拉框选择技能
- 自动添加分配

**方式 B：快速批量分配（推荐）** ⭐
- 在卡片左上打勾选中多个关卡
- 在工具栏"快速分配"区选择技能
- 点击 **`分配给 N 个关卡`**
- 一次性分配完成

### 调整技能参数

对于已分配的卡片，可以调整：
- **Skill**：改成其他技能
- **教学模式**：guided（引导）/ challenge（挑战）/ demo（演示）
- **难度 1-6**：表示这个技能在本关卡中的难度梯度

### 导出关卡映射

1. 点击右上角 **`导出`** 按钮
2. 保存为 `level_skill_map.json`
3. 把这个文件**覆盖**到 App 项目的 `data/skills/` 目录

---

### 步骤 3️⃣：完成！

1. App 项目现在有两个新文件：
   - `data/skills/skill_graph_cfop.json` （技能树）
   - `data/skills/level_skill_map.json` （关卡映射）

2. **直接运行 App** → 系统会自动加载这两个文件
3. 完整的技能教学系统已可用！

---

## 常见问题

### Q: 为什么关卡映射页打开是空的？
**A**: 先确保你已经点击过"关卡编辑"标签并加载了关卡。关卡映射页会自动显示所有 51 个关卡。

### Q: 怎样快速给某个阶段的所有关卡都分配技能？
**A**: 
1. 先在"按阶段筛选"选择该阶段
2. 可以看到只有这个阶段的关卡
3. 用快速批量分配功能选中多个，一起分配

### Q: 编辑后没看到 "保存" 按钮？
**A**: 确认你确实修改了内容。如果修改生效但 "保存" 按钮仍未出现，可能是自动保存已完成。

### Q: 导出的 JSON 文件放在哪里？
**A**: 点击"导出"按钮时会弹出系统文件保存对话框，你自己选择保存位置。建议直接保存到 App 项目的 `data/skills/` 目录。

---

## 提示 💡

- 🎯 **从技能编辑开始**：先把技能树设置好，再分配给关卡
- ⚡ **用快速批量分配**：比逐个分配快 10 倍
- 📱 **响应式设计**：在不同窗口大小都能正常使用
- 💾 **定期导出**：完成一个阶段的工作就导出一次，防止丢失

---

有问题？再看一遍前面的步骤，或者检查错误提示信息。
